<span id="restocking_fieldset">
  <%= form_for :stock_record, :url => admin_restock_path, :html => { :id => "restocking_form" } do |f| %>
    <%= f.label :quantity, t(:quantity) %>
    <%= f.text_field :quantity, :class => 'small' %>
    <%= f.label :container_taxon_id, t(:container_taxon) %>
    <%= f.select :container_taxon_id, options_from_collection_for_select(@container_taxons, "id", "name") %>
    <%= hidden_field :stock_record, :variant_id, :value => @variant.id %>
    <%= f.submit t(:restock), :html => {:class => 'button', :id => 'restocking_form_submit'} %>
  <% end %>
</div>
