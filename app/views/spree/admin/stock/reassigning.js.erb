$(document).ready(function(){
  $('td#reassigning_<%= @variant.id %>_<%= @container_taxon_id %>').html('<%= escape_javascript(render :partial => "reassigning_form", :formats => [:html], :handlers => [:erb]) %>');
});