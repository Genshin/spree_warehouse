
<%= render :partial => 'spree/admin/shared/warehouse_sub_menu' %>

<div class="toolbar" data-hook="toolbar">
  <ul class="actions">
    <li id="new_visual_code_type_link">
      <p><%= button_link_to t("visual_code_type.new"), new_admin_visual_code_type_path, :icon => 'add', :id => 'admin_new_visual_code_type' %></p>
    </li>
  </ul>
  <br class="clear" />
</div>

<h1><%= t("visual_code.types") %></h1>

<table id="visual_code_types" class="index">
  <tr>
	<th><%= t 'visual_code_type.name' %></th>
    <th></th>
  </tr>

<% @visual_code_types.each do |vct| %>
  <tr>
    <td><%= vct.name %></td>
    <td class="actions">
      <% if vct.can_delete? %>
        <%= link_to_edit vct %>
        <%= link_to_delete vct %>
      <% end %>
    </td>
  </tr>
<% end %>
</table>
